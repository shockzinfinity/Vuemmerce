/* export default function ({ store, error }) {
  console.log(store)
  if (store.state.userInfo.role !== 1) {
    error({
      message: 'You are not connected',
      statusCode: 403
    })
  }
}
 */
